{
  "name": "nba-fantasy-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build:snapshot": "node ../scripts/build-season-summary-snapshot.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.8.0"
  }
}
